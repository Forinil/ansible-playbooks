---
- name: Provision Pi-hole server
  hosts: '{{ target }}'
  # become: yes
  # become_method: sudo
  remote_user: root
  vars_files:
    - vars/pihole-config.yml
  tasks:
    - import_tasks: tasks/system-settings.yml
    - import_tasks: tasks/users-and-groups.yml
    - import_tasks: tasks/configure-cloudflared-dns-proxy.yml
    - import_tasks: tasks/configure-tailscale.yml
    - import_tasks: tasks/configure-pihole.yml